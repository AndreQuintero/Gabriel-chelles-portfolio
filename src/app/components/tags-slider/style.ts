import styled from 'styled-components'

export const Wrapper = styled.div`
  .slick-slide {
    width: auto !important;
    margin: 0.8rem;
  }

  .slick-track {
    display: flex;
    margin: 0 -17%; /* Adjust the negative margin to control the gap size for rows and columns */
  }
`
